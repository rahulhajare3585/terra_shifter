import 'package:flutter/material.dart';

class JcbInvoiceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Invoice'),
      ),
      body: Center(
        child: Text('Invoice Screen'),
      ),
    );
  }

}